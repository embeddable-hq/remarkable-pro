---
'@embeddable.com/remarkable-pro': minor
---

MultiSelectFieldPro: add an opt-in select all / deselect all option, controlled by a new 'Display "select / deselect all"' input (off by default). Even when enabled it only appears when the loaded values are fewer than the maximum options limit, so the full list is known to be complete
