---
'@embeddable.com/remarkable-pro': patch
---

Pivot table: renamed the "Show row/column aggregation" inputs to "Show row/column calculation", and the Sum/Min/Max/Average total labels shown in the rendered table are now translatable via the theme i18n (`charts.pivotTable.sum|min|max|average`).
