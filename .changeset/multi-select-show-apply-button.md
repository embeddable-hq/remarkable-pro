---
'@embeddable.com/remarkable-pro': minor
---

Add a "Show apply button" input to the Multi Select Field (default on). When disabled, the apply button is hidden and ticking a value applies the selection immediately through the existing onChange event and variable.
